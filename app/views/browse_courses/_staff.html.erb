<% provide(:title, @staff.name) %>

<div class="page-header">
	<h1>
		<%= @staff.name %> 
		<small>
			<a href="<%= @staff.url %>"><%= @staff.url %></a>
		</small>
	</h1>
</div>

<h3>Courses given by <%= @staff.name %>:</h3>

<%= render 'search_form' %>