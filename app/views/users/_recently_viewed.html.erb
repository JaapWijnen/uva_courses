<div class="container">	
	Lijst met vakken laatst 10 bekeken vakken
	<ul>
		<% @user ||= current_user %>
		<% @user.recently_viewed_items.each do |item| %>
			<li><%= link_to item.course.name, controller: 'browse_courses', action: 'show', id: item.course.id, type: 'courses' %></li>
		<% end %>
	</ul>
</div>